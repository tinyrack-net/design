// Generated by scripts/generate-showcase-stories.mjs
import '../../../src/showcase/showcase.css';
import type { Meta, StoryObj } from '@storybook/react-vite';
import type { ShowcaseScenarioId } from '../../../src/showcase/index.js';
import {
  mantineShowcaseEntries,
  SingleShowcaseStory,
} from '../../../src/showcase/index.js';

type ComponentStoryProps = {
  scenarioId?: ShowcaseScenarioId;
};

const entry = mantineShowcaseEntries.find(
  (showcaseEntry) => showcaseEntry.id === 'mantine-popover',
);

if (!entry) {
  throw new Error('Missing showcase entry: mantine-popover');
}

function PopoverStory({ scenarioId = 'preview' }: ComponentStoryProps) {
  return (
    <SingleShowcaseStory entry={entry} library="mantine" scenarioId={scenarioId} />
  );
}

const meta = {
  title: 'Mantine/Components/Popover',
  component: PopoverStory,
  args: {
    scenarioId: 'preview',
  },
  argTypes: {
    scenarioId: {
      control: 'select',
      options: ['preview', 'variants'],
      description:
        'Switch between the default component preview and the variant matrix.',
    },
  },
  parameters: {
    layout: 'centered',
  },
} satisfies Meta<typeof PopoverStory>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Preview: Story = {
  args: {
    scenarioId: 'preview',
  },
};

export const Variants: Story = {
  args: {
    scenarioId: 'variants',
  },
};
