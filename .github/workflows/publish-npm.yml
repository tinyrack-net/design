name: Publish npm package

on:
  push:
    tags:
      - "ui-v*.*.*"

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish @tinyrack/ui
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v7.0.1

      - name: Set up JavaScript runtime
        uses: ./.github/actions/setup-js
        with:
          registry-url: https://registry.npmjs.org

      - name: Install UI dependencies
        run: pnpm --filter @tinyrack/ui... install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm --filter @tinyrack/ui exec playwright install --with-deps chromium firefox webkit

      - name: Verify tag matches package version
        run: |
          package_version="ui-v$(node -p "require('./packages/ui_web/package.json').version")"
          if [ "$package_version" != "$GITHUB_REF_NAME" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match package version $package_version"
            exit 1
          fi

      - name: Test package
        run: pnpm --filter @tinyrack/ui test

      - name: Publish package to npm
        run: pnpm --config.ignore-scripts=true --dir packages/ui_web publish --provenance --access public --no-git-checks
