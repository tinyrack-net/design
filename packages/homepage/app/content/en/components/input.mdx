---
title: "Input"
description: "A tokenized native input that preserves labels, refs, validity, and form submission."
section: components
group: forms-inputs
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/input.demo.js';

## Contract

`TRInput` renders a native `<input>` and preserves its props, ref, events, validity, and form behavior. Wrap it in `TRField.Root` and add a `TRField.Label`: the field associates the label with the input, so you do not write `id` or `htmlFor` yourself. Use `TRField.Description` for instructions and `TRField.Error` for validation messages, which the field links through `aria-describedby`.

Use `uiSize="sm"` for dense controls, the default `md` for forms, and `lg` where a larger target is useful. `disabled`, `readOnly`, and `required` retain their native semantics and Tinyrack state styles. Set `disabled` or `invalid` on `TRField.Root` to drive the whole field, or keep `aria-invalid` on the input when there is no surrounding field.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/input.css';"],
      codeImports: ["import { TRInput } from '@tinyrack/ui/components/input';","import { TRButton } from '@tinyrack/ui/components/button';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="input-basic"
  title="Labeled input"
  preview={<div data-docs-example-item-count="1"><Stories.InputPreview defaultValue="" disabled={false} label="Rack name" placeholder="rack-alpha" readOnly={false} required={false} /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRField } from '@tinyrack/ui/components/field';
import { TRInput } from '@tinyrack/ui/components/input';

export function RackNameInput() {
  return (
    <TRField.Root className="w-80 max-w-full">
      <TRField.Label>Rack name</TRField.Label>
      <TRInput name="rack" placeholder="rack-alpha" />
    </TRField.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/input.css';" },
    },
  ]}
/>

## Examples

<ComponentExampleTabs
  id="input-sizes"
  title="Sizes"
  preview={<div data-docs-example-item-count="3"><Stories.InputSizeComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRField } from '@tinyrack/ui/components/field';
import { TRInput } from '@tinyrack/ui/components/input';

export function InputSizes() {
  return (
    <div className="grid gap-4">
      <TRField.Root>
        <TRField.Label>SM input</TRField.Label>
        <TRInput placeholder="rack-alpha" uiSize="sm" />
      </TRField.Root>
      <TRField.Root>
        <TRField.Label>MD input</TRField.Label>
        <TRInput placeholder="rack-alpha" />
      </TRField.Root>
      <TRField.Root>
        <TRField.Label>LG input</TRField.Label>
        <TRInput placeholder="rack-alpha" uiSize="lg" />
      </TRField.Root>
    </div>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/input.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="input-states"
  title="Input states"
  preview={<div data-docs-example-item-count="4"><Stories.InputStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRField } from '@tinyrack/ui/components/field';
import { TRInput } from '@tinyrack/ui/components/input';

export function InputStates() {
  return (
    <div className="grid gap-4 sm:grid-cols-2">
      <TRField.Root>
        <TRField.Label>Default</TRField.Label>
        <TRInput defaultValue="rack-alpha" />
      </TRField.Root>
      <TRField.Root disabled>
        <TRField.Label>Disabled</TRField.Label>
        <TRInput defaultValue="rack-disabled" />
      </TRField.Root>
      <TRField.Root>
        <TRField.Label>Read only</TRField.Label>
        <TRInput defaultValue="rack-locked" readOnly />
      </TRField.Root>
      <TRField.Root invalid>
        <TRField.Label>Invalid</TRField.Label>
        <TRInput defaultValue="rack duplicate" required />
      </TRField.Root>
    </div>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/input.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="input-validation"
  title="Required submission and recovery"
  preview={<div data-docs-example-item-count="1"><Stories.InputValidationPreview /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRButton } from '@tinyrack/ui/components/button';
import { TRField } from '@tinyrack/ui/components/field';
import { TRForm } from '@tinyrack/ui/components/form';
import { TRInput } from '@tinyrack/ui/components/input';
import { useRef, useState } from 'react';

export function RequiredInput() {
  const inputRef = useRef<HTMLInputElement>(null);
  const [attempted, setAttempted] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const [value, setValue] = useState('');
  const invalid = attempted && value.trim().length === 0;

  return (
    <TRForm
      noValidate
      onSubmit={(event) => {
        event.preventDefault();
        const valid = event.currentTarget.checkValidity();
        setAttempted(true);
        setSubmitted(valid);
        if (!valid) inputRef.current?.focus();
      }}
    >
      <TRField.Root invalid={invalid}>
        <TRField.Label>Rack name</TRField.Label>
        <TRInput
          name="rack"
          onValueChange={(nextValue) => {
            setValue(nextValue);
            setSubmitted(false);
          }}
          onInvalid={(event) => event.preventDefault()}
          ref={inputRef}
          required
          value={value}
        />
        {invalid ? <TRField.Error match>Rack name is required.</TRField.Error> : null}
      </TRField.Root>
      <TRButton type="submit">Continue</TRButton>
      <output aria-live="polite">
        {submitted ? 'Ready to create ' + value + '.' : ''}
      </output>
    </TRForm>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/components/button.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/form.css';\n@import '@tinyrack/ui/components/input.css';" },
    },
  ]}
/>

## API

`TRInputProps` extends Base UI Input props and adds `uiSize?: 'sm' | 'md' | 'lg'` with a default of `md`. It forwards native input attributes such as `name`, `type`, `autoComplete`, `disabled`, `readOnly`, and `required`, plus `ref`, `className`, `style`, and event handlers.

Use `value` with `onValueChange(value, details)` for controlled text, or `defaultValue` for native uncontrolled and form-reset behavior. Use native `onChange` when the event object matters. `aria-invalid="true"`, Base UI `data-invalid`, and native `:invalid` all activate the invalid border. The `render` prop replaces the rendered element while Base UI merges behavior, state, ref, and `.tr-input` styling onto it.
