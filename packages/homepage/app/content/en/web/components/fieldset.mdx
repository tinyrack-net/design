---
title: "Fieldset"
description: "Groups related controls under one native, accessible legend."
section: components
group: forms-inputs
---
import { ComponentPlayground } from '../../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../../documentation/shared/component-install.js';
import * as Stories from '../../../../documentation/components/fieldset.demo.js';

## Contract

`Root` renders a native `<fieldset>`. `Legend` renders its native `<legend>` by default, giving the group an accessible name. Each child control still needs its own visible label: put the control and a `TRField.Label` in a `TRField.Item`, which scopes that label to the control inside it — no `id` or `htmlFor` required. Every `TRField.Item` needs a `TRField.Root` ancestor, so group the items under one `TRField.Root` inside `Root` and give that root a `gap` override to keep the fieldset's spacing. Setting `disabled` on `Root` uses native form-control propagation.

## Install

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/fieldset.css';"],
      codeImports: ["import { TRFieldset } from '@tinyrack/ui/components/fieldset';"] }]} />

## Playground

<ComponentPlayground definition={Stories.playground} />

## Usage

<ComponentExampleTabs
  id="fieldset-basic"
  title="Notification channels"
  preview={<Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts legend="Notifications" disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function NotificationChannels() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>Notifications</TRFieldset.Legend>
      <TRField.Root className="gap-3">
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            Email alerts
          </TRField.Label>
        </TRField.Item>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            Incident summaries
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
    </TRFieldset.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

## Examples

<ComponentExampleTabs
  id="fieldset-states"
  title="Enabled and disabled groups"
  preview={<div className="grid gap-5 sm:grid-cols-2"><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled={false} legend="Editable settings" /><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled legend="Managed settings" /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

function NotificationSettings({
  disabled = false,
  legend,
}: {
  disabled?: boolean;
  legend: string;
}) {
  return (
    <TRFieldset.Root disabled={disabled}>
      <TRFieldset.Legend>{legend}</TRFieldset.Legend>
      <TRField.Root className="gap-3">
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            Email alerts
          </TRField.Label>
        </TRField.Item>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            Incident summaries
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
    </TRFieldset.Root>
  );
}

export function FieldsetStates() {
  return (
    <div className="grid gap-5 sm:grid-cols-2">
      <NotificationSettings legend="Editable settings" />
      <NotificationSettings disabled legend="Managed settings" />
    </div>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="fieldset-composition"
  title="Nested groups"
  preview={<div data-docs-example-item=""><Stories.FieldsetCompositionExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function IncidentNotifications() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>Incident notifications</TRFieldset.Legend>
      <TRField.Root>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            Enable incident notifications
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
      <TRFieldset.Root>
        <TRFieldset.Legend>Delivery channels</TRFieldset.Legend>
        <TRField.Root className="gap-3">
          <TRField.Item className="items-center">
            <TRCheckbox.Root defaultChecked>
              <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
            </TRCheckbox.Root>
            <TRField.Label className="normal-case tracking-normal font-normal">
              Email
            </TRField.Label>
          </TRField.Item>
          <TRField.Item className="items-center">
            <TRCheckbox.Root>
              <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
            </TRCheckbox.Root>
            <TRField.Label className="normal-case tracking-normal font-normal">
              SMS
            </TRField.Label>
          </TRField.Item>
        </TRField.Root>
      </TRFieldset.Root>
    </TRFieldset.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

## API

`TRFieldsetRoot` and `TRFieldsetLegend` are exported individually and as `TRFieldset.Root` and `TRFieldset.Legend`.

| Part | Public contract |
| --- | --- |
| `Root` | Native fieldset attributes, `disabled`, `render`, `className`, `style`, events, and ref |
| `Legend` | Native legend by default; accepts `render`, `className`, `style`, events, and ref |

Use `render` only when native `<legend>` placement is unsuitable. Base UI keeps a custom-rendered legend associated with the fieldset through `aria-labelledby`.
