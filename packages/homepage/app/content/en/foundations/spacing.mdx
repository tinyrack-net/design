---
title: "Spacing"
description: "Choose spacing by relationship: details stay close, components breathe, and sections mark task boundaries."
section: foundations
order: 3
---
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackSpacing } from '@tinyrack/ui/core';

## Start with relationships

Spacing explains which elements belong together. Choose the relationship before the number, and use one repeated gap for peers.

- **Detail spacing (`xs`–`sm`):** icon and label, label and value, or lines inside one compact group.
- **Component spacing (`md`–`lg`):** fields, actions, rows, and padding inside a bounded component.
- **Section spacing (`xl`–`2xl`):** task groups, page regions, and major changes in subject.

## Visual scale

<div className="grid gap-3" role="list">{Object.entries(tinyrackSpacing).map(([name, value]) => (<div className="grid grid-cols-[3rem_minmax(0,1fr)_5rem] items-center gap-3" key={name} role="listitem"><TRCode>{name}</TRCode><span className="block h-3 bg-tinyrack-primary" style={{ width: value }} /><TRCode>{value}</TRCode></div>))}</div>

## Layout rhythm

Build from the inside out: group label and value with `xs`, separate peer rows with `sm` or `md`, pad the component with `lg`, then separate it from the next task with `xl` or `2xl`. Avoid a different gap for every pair; repeated relationships should look repeated.

<div className="grid gap-tinyrack-xl md:grid-cols-2"><section className="grid gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg"><div className="grid gap-tinyrack-xs"><span className="text-tinyrack-xs text-tinyrack-text-muted">Rack</span><strong>rack-a</strong></div><div className="grid gap-tinyrack-xs"><span className="text-tinyrack-xs text-tinyrack-text-muted">Status</span><strong>Healthy</strong></div></section><section className="grid content-start gap-tinyrack-sm"><h3 className="m-0">Rhythm</h3><p className="m-0 text-tinyrack-text-muted">Detail gaps bind each label and value. Component gaps separate rows. Section gaps separate tasks.</p></section></div>

## Implementation and reference

```css
.rack-card { display: grid; gap: var(--tinyrack-space-md); padding: var(--tinyrack-space-lg); }
```

<TRTable.Root containerProps={{ 'aria-label': 'Spacing token reference', tabIndex: 0 }} data-foundation-reference="spacing"><thead><tr><th scope="col">Token</th><th scope="col">Value</th><th scope="col">Typical relationship</th><th scope="col">Tailwind</th><th scope="col">TypeScript</th></tr></thead><tbody>{Object.entries(tinyrackSpacing).map(([token, value]) => (<tr key={token}><th scope="row"><TRCode>{token}</TRCode></th><td><TRCode>{value}</TRCode></td><td>{token === '3xs' || token === 'xs' || token === 'sm' ? 'Detail' : token === 'md' || token === 'lg' ? 'Component' : 'Section'}</td><td><TRCode>{`gap-tinyrack-${token}`}</TRCode></td><td><TRCode>{`tinyrackSpacing['${token}']`}</TRCode></td></tr>))}</tbody></TRTable.Root>

See the [Tailwind spacing mappings](/en/foundations/tailwind/#spacing-controls).
