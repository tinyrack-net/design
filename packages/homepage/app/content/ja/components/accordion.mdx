---
title: "Accordion"
description: "関連コンテンツを段階的に表示するキーボード対応セクションです。Tab と Shift+Tab でトリガー間を移動し、Enter または Space でフォーカス中のセクションを切り替えます。"
section: components
order: 0
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRAccordion } from '@tinyrack/ui/components/accordion';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/accordion.demo.js';

## コントラクト

| 軸 | 値 | 既定値 |
| --- | --- | --- |
| selection | controlled `value` or uncontrolled `defaultValue` | `[]` |
| expansion | Root `multiple` | single |
| availability | Root `disabled` or Item `disabled` | interactive |
| lifecycle | unmount, `keepMounted`, or `hiddenUntilFound` | unmount |

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/accordion.css';", "import { TRAccordion } from '@tinyrack/ui/components/accordion';"] }]} />

## プレイグラウンド

Playground では Root disabled、Item disabled、単一または複数展開、3 種類の panel lifecycle を操作できます。制御された `value` は preview の操作に従い、Reset は Overview だけが開き、閉じた panel を unmount する初期状態を復元します。

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="accordion-basic"
  title="制御付き複数展開"
  description="どちらの section も開閉でき、下の結果が現在の value を反映します。"
  previewLayout="stretch"
  preview={<div data-docs-example-item-count="1"><Stories.AccordionInteractiveExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import '@tinyrack/ui/components/accordion.css';
import { TRAccordion } from '@tinyrack/ui/components/accordion';

export function AccordionExample() {
  const [value, setValue] = useState<string[]>(['overview']);

  return (
    <div className="grid gap-3">
      <TRAccordion.Root multiple onValueChange={setValue} value={value}>
        <TRAccordion.Item value="overview">
          <TRAccordion.Header>
            <TRAccordion.Trigger>What is Tinyrack?</TRAccordion.Trigger>
          </TRAccordion.Header>
          <TRAccordion.Panel>A React-only UI system.</TRAccordion.Panel>
        </TRAccordion.Item>
        <TRAccordion.Item value="install">
          <TRAccordion.Header>
            <TRAccordion.Trigger>How do I install it?</TRAccordion.Trigger>
          </TRAccordion.Header>
          <TRAccordion.Panel>Install the package and component CSS.</TRAccordion.Panel>
        </TRAccordion.Item>
      </TRAccordion.Root>
      <output aria-live="polite">
        Expanded: {value.length === 0 ? 'none' : value.join(', ')}
      </output>
    </div>
  );
}` }]}
/>

## 例

<ComponentExampleTabs
  id="accordion-expansion-states"
  data-docs-example-item-count="2"
  title="single、multiple、disabled、persistent panel"
  previewLayout="stretch"
  preview={<div className="grid gap-5"><TRAccordion.Root data-docs-example-item="" defaultValue={['single']}><TRAccordion.Item value="single"><TRAccordion.Header><TRAccordion.Trigger>単一展開</TRAccordion.Trigger></TRAccordion.Header><TRAccordion.Panel>別の item を開くとこの item が閉じます。</TRAccordion.Panel></TRAccordion.Item><TRAccordion.Item disabled value="disabled"><TRAccordion.Header><TRAccordion.Trigger>利用できない item</TRAccordion.Trigger></TRAccordion.Header><TRAccordion.Panel>利用できない詳細。</TRAccordion.Panel></TRAccordion.Item></TRAccordion.Root><TRAccordion.Root data-docs-example-item="" defaultValue={['network', 'storage']} multiple><TRAccordion.Item value="network"><TRAccordion.Header><TRAccordion.Trigger>ネットワーク</TRAccordion.Trigger></TRAccordion.Header><TRAccordion.Panel>10 Gbps uplink。</TRAccordion.Panel></TRAccordion.Item><TRAccordion.Item value="storage"><TRAccordion.Header><TRAccordion.Trigger>ストレージ</TRAccordion.Trigger></TRAccordion.Header><TRAccordion.Panel keepMounted>72% 利用可能。閉じてもこの panel は mount されたままです。</TRAccordion.Panel></TRAccordion.Item></TRAccordion.Root></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import '@tinyrack/ui/components/accordion.css';
import { TRAccordion } from '@tinyrack/ui/components/accordion';

export function AccordionStates() {
  return <div className="grid gap-5">
  <TRAccordion.Root defaultValue={['single']}>
    <TRAccordion.Item value="single">
      <TRAccordion.Header>
        <TRAccordion.Trigger>Single expansion</TRAccordion.Trigger>
      </TRAccordion.Header>
      <TRAccordion.Panel>Opening another item closes this one.</TRAccordion.Panel>
    </TRAccordion.Item>
    <TRAccordion.Item disabled value="disabled">
      <TRAccordion.Header>
        <TRAccordion.Trigger>Unavailable item</TRAccordion.Trigger>
      </TRAccordion.Header>
      <TRAccordion.Panel>Unavailable detail.</TRAccordion.Panel>
    </TRAccordion.Item>
  </TRAccordion.Root>

  <TRAccordion.Root defaultValue={['network', 'storage']} multiple>
    <TRAccordion.Item value="network">
      <TRAccordion.Header><TRAccordion.Trigger>Network</TRAccordion.Trigger></TRAccordion.Header>
      <TRAccordion.Panel>10 Gbps uplink.</TRAccordion.Panel>
    </TRAccordion.Item>
    <TRAccordion.Item value="storage">
      <TRAccordion.Header><TRAccordion.Trigger>Storage</TRAccordion.Trigger></TRAccordion.Header>
      <TRAccordion.Panel keepMounted>
        72% available. This panel stays mounted when closed.
      </TRAccordion.Panel>
    </TRAccordion.Item>
  </TRAccordion.Root>
</div>;
}` }]}
/>

## API

### 構造

```tsx
<TRAccordion.Root>
  <TRAccordion.Item value="network">
    <TRAccordion.Header>
      <TRAccordion.Trigger>ネットワーク</TRAccordion.Trigger>
    </TRAccordion.Header>
    <TRAccordion.Panel>オンライン</TRAccordion.Panel>
  </TRAccordion.Item>
</TRAccordion.Root>
```

完全な構造は `Root`、`Item`、`Header`、`Trigger`、`Panel` です。各パートは `TRAccordion.Part` 形式と名前付きの `TRAccordionPart` エクスポート形式の両方で利用できます。対応する `TRAccordionPartProps` 型と `TRAccordionPartState` 型も提供されます。

### Root のプロパティ

| Prop | Type | 既定値 | 用途 |
| --- | --- | --- | --- |
| `value` | `Value[]` | - | 開いている item value を制御します。 |
| `defaultValue` | `Value[]` | `[]` | 非制御状態で最初に開く item を指定します。 |
| `onValueChange` | `(value, details) => void` | - | 次の value と Base UI event details を通知します。 |
| `multiple` | `boolean` | `false` | 複数の item を同時に開いたままにします。 |
| `disabled` | `boolean` | `false` | すべての item の操作を無効にします。 |
| `keepMounted` | `boolean` | `false` | 閉じた panel を `hidden` のまま DOM に保持します。 |
| `hiddenUntilFound` | `boolean` | `false` | 閉じた panel を `hidden="until-found"` で検索可能に保ち、`keepMounted` より優先されます。 |
| `render` | `ReactElement \| function` | `<div>` | 動作と ref のマージを保ったまま root element を置き換えます。 |

`Item` には制御付きまたは初期展開のための安定した `value` が必要で、`disabled` と `onOpenChange` を受け取ります。`Header` は既定で `h3` をレンダリングするため、周囲の見出しレベルに合わせて `render` を使用してください。`Trigger` はボタン、`Panel` は関連付けられたリージョンをレンダリングします。すべてのパートは `className`、`style`、ネイティブ属性、イベントハンドラー、ref、Base UI の状態に基づく `render` コントラクトを保持します。

Enter または Space はフォーカスを移動せずにトリガーを切り替えます。Tab と Shift+Tab は文書内の通常の順序に従います。無効なトリガーも `aria-disabled="true"` でフォーカスできますが、切り替わりません。非推奨の矢印キーロービングフォーカス用プロパティは、フォーカス動作に影響しません。

### 外観

| Token | Fallback | 用途 |
| --- | --- | --- |
| `--tr-accordion-background` | `--tinyrack-surface` | Root の背景。 |
| `--tr-accordion-radius` | `--tinyrack-radius-md` | 外側の radius. |
| `--tr-accordion-content-background` | `transparent` | 開いた Panel の背景。 |
| `--tr-accordion-disabled-background` | `--tinyrack-surface-muted` | 無効な Item の背景。 |
| `--tr-accordion-disabled-color` | `--tinyrack-text-muted` | 無効な Item の文字色。 |
| `--tr-collapsible-duration` | `--tinyrack-duration-normal` | Collapsible から継承する panel transition duration. |

`@tinyrack/ui/components/accordion.css` を明示的に import してください。Foundation token が light/dark color、border、focus indicator、typography、spacing、reduced-motion 動作を提供します。
