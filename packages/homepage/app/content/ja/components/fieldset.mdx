---
title: "Fieldset"
description: "関連するコントロールを、ネイティブでアクセシブルな 1 つの legend の下にまとめます。"
section: components
group: forms-inputs
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/fieldset.demo.js';

## 主なプロパティ

`Root` はネイティブの `<fieldset>` をレンダリングします。`Legend` はデフォルトでネイティブの `<legend>` をレンダリングし、グループにアクセシブルな名前を付けます。各子コントロールには固有の表示ラベルが必要です。コントロールと `TRField.Label` を `TRField.Item` にまとめると、アイテムが内側のコントロールにラベルを結び付けるため、`id` や `htmlFor` は不要です。`TRField.Item` には `TRField.Root` の祖先が必須です。アイテムは 1 つの `TRField.Root` にまとめて `Root` の中に置き、フィールドセットの間隔を保つにはそのルートに `gap` を指定します。`Root` の `disabled` は、ネイティブのフォームコントロールへの無効状態の伝播を利用します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/fieldset.css';"],
      codeImports: ["import { TRFieldset } from '@tinyrack/ui/components/fieldset';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  id="fieldset-basic"
  title="通知チャンネル"
  preview={<Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts legend="通知" disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function NotificationChannels() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>通知</TRFieldset.Legend>
      <TRField.Root className="gap-3">
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            メールアラート
          </TRField.Label>
        </TRField.Item>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            インシデントの概要
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
    </TRFieldset.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

## 例

<ComponentExampleTabs
  id="fieldset-states"
  title="有効・無効のグループ"
  preview={<div className="grid gap-5 sm:grid-cols-2"><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled={false} legend="編集可能な設定" /><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled legend="管理された設定" /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

function NotificationSettings({
  disabled = false,
  legend,
}: {
  disabled?: boolean;
  legend: string;
}) {
  return (
    <TRFieldset.Root disabled={disabled}>
      <TRFieldset.Legend>{legend}</TRFieldset.Legend>
      <TRField.Root className="gap-3">
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            メールアラート
          </TRField.Label>
        </TRField.Item>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            インシデントの概要
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
    </TRFieldset.Root>
  );
}

export function FieldsetStates() {
  return (
    <div className="grid gap-5 sm:grid-cols-2">
      <NotificationSettings legend="編集可能な設定" />
      <NotificationSettings disabled legend="管理された設定" />
    </div>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="fieldset-composition"
  title="入れ子のグループ"
  preview={<div data-docs-example-item=""><Stories.FieldsetCompositionExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function IncidentNotifications() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>インシデント通知</TRFieldset.Legend>
      <TRField.Root>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            インシデント通知を有効にする
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
      <TRFieldset.Root>
        <TRFieldset.Legend>配信チャネル</TRFieldset.Legend>
        <TRField.Root className="gap-3">
          <TRField.Item className="items-center">
            <TRCheckbox.Root defaultChecked>
              <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
            </TRCheckbox.Root>
            <TRField.Label className="normal-case tracking-normal font-normal">
              メール
            </TRField.Label>
          </TRField.Item>
          <TRField.Item className="items-center">
            <TRCheckbox.Root>
              <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
            </TRCheckbox.Root>
            <TRField.Label className="normal-case tracking-normal font-normal">
              SMS
            </TRField.Label>
          </TRField.Item>
        </TRField.Root>
      </TRFieldset.Root>
    </TRFieldset.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

## API

`TRFieldsetRoot` と `TRFieldsetLegend` は個別にエクスポートされ、`TRFieldset.Root` と `TRFieldset.Legend` としても利用できます。

| パーツ | 説明 |
| --- | --- |
| `Root` | ネイティブ fieldset 属性、`disabled`、`render`、`className`、`style`、イベント、ref |
| `Legend` | デフォルトはネイティブ legend。`render`、`className`、`style`、イベント、ref に対応 |

ネイティブの `<legend>` の配置が適さない場合に限り `render` を使用してください。カスタムレンダリングした legend も、Base UI が `aria-labelledby` で fieldset と関連付けます。
