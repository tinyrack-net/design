---
title: "Docs Shell"
description: "route 状態、スクロール、レイアウトモードに対応するレスポンシブなドキュメント chrome です。"
section: components
order: 55
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/docs-shell.demo.js';

## コントラクト

`Root`、`Header`、`Brand`、`Actions`、`Sidebar`、`Main`、`Outline` でレスポンシブな shell を構成します。`docs` は desktop navigation と focus が管理された mobile drawer を提供し、`splash` は global header だけを残し、`standalone` は両方の navigation 領域を取り除きます。Router 状態は props だけで渡されます。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/docs-shell.css';", "import { TRDocsShell } from '@tinyrack/ui/components/docs-shell';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs
  description="ヘッダー、アクション、ナビゲーション、本文、アウトラインからなる完全な構造を、そのまま利用できるシェルとして示します。プレビューのサイズを変えると、モバイル用ドロワーを確認できます。"
  id="docs-shell-basic"
  data-docs-example-item-count="1"
  preview={<Stories.DocsShellDocsPreview />}
  previewClassName="!min-h-[34rem] !min-w-0 !p-0"
  previewLayout="stretch"
  sources={[{ label: 'React', language: 'tsx', code: Stories.docsShellCompositionSource }]}
  title="ドキュメント構成"
/>

## 例

<ComponentExampleTabs
  description="ヘッダーだけのランディングページには `splash`、ドキュメントナビゲーションのないコンテンツには `standalone` を使用します。"
  id="docs-shell-layouts"
  data-docs-example-item-count="3"
  preview={<Stories.DocsShellLayoutMatrix />}
  previewClassName="!min-h-96 !min-w-0 !p-0"
  previewLayout="stretch"
  sources={[{ label: 'React', language: 'tsx', code: Stories.docsShellLayoutsSource }]}
  title="ドキュメントレイアウト"
/>

## API

`Root` には `currentPath` と `locationKey` が必要です。Shell 内の heading scroll には `hash`、scroll 復元には `POP`、`PUSH`、`REPLACE` のいずれかの `navigationKind`、pending progress と `aria-busy` の表示には `pendingPath` を渡します。`layout` は `docs`、`splash`、`standalone` から選びます。

`Header`、`Brand`、`Actions`、`Sidebar`、`Outline` は native props、class name、style、ref を保持します。`Main` は inner content 用の `contentClassName` と scroll region の accessible name 用の `viewportLabel` も受け取ります。

制限された preview や application 領域に shell を埋め込む場合は `--tr-docs-shell-block-size` を設定してください。既定値は `100dvh` です。
