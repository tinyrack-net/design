---
title: "Drawer"
description: "ビューポートの端からスライドし、開閉時に backdrop をフェードするスワイプ対応のタスクサーフェスです。"
section: components
order: 31
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { TRDrawer } from '@tinyrack/ui/components/drawer';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/drawer.demo.js';

## コントラクト

`TRDrawer` は Base UI の controlled/uncontrolled state、swipe と snap-point gesture、focus 管理、keyboard dismissal、portal、React 19 ref と `render` prop を保ちます。motion は `swipeDirection` と reduced-motion 設定に従います。Playground は drawer event で `open` と `snapPoint` を同期し、Reset は閉じた full-height downward の既定値を復元します。

## インストール

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/drawer.css';", "import { TRDrawer } from '@tinyrack/ui/components/drawer';"] }]} />

## プレイグラウンド

<ComponentPlayground definition={Stories.playground} />

## 使用方法

<ComponentExampleTabs id="drawer-basic" title="既定" preview={<div data-docs-example-item=""><TRDrawer.Root><TRDrawer.Trigger>設定を開く</TRDrawer.Trigger><TRDrawer.Portal><TRDrawer.Backdrop /><TRDrawer.Viewport><TRDrawer.Popup><TRDrawer.Content><TRDrawer.Title>ラック設定</TRDrawer.Title><TRDrawer.Description>デプロイ設定を更新します。</TRDrawer.Description><TRDrawer.Close>閉じる</TRDrawer.Close></TRDrawer.Content></TRDrawer.Popup></TRDrawer.Viewport></TRDrawer.Portal></TRDrawer.Root></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.drawerBasicSourceJa }]} />

## 例

<ComponentExampleTabs id="drawer-states" data-docs-example-item-count="1" title="スナップポイントと入れ子のコンテンツ" description="`compact`、`medium`、`full-height` の位置間をドラッグします。入れ子のドロワーでは、フォーカスを内部に留める動作と連動するインデントを確認できます。" preview={<Stories.DrawerPreview activeSnapPoint="compact" label="スナップドロワーを開く" open={false} swipeDirection="down" />} sources={[{ label: 'React', language: 'tsx', code: Stories.drawerSnapPointsSourceJa }]} />

<ComponentExampleTabs id="drawer-directions" data-docs-example-item-count="4" title="方向" description="`swipeDirection` は `down`、`up`、`left`、`right` に設定できます。ポップアップは対応するビューポート端に接し、同じ方向へ閉じます。" preview={<Stories.DrawerDirectionPreview />} sources={[{ label: 'React', language: 'tsx', code: Stories.drawerDirectionsSourceJa }]} />

<ComponentExampleTabs id="drawer-provider-handle" data-docs-example-item-count="1" title="Provider、外部ハンドル、スワイプ領域" description="Provider はページのインデントを調整します。型付きの外部トリガーは `createHandle` でペイロードを渡し、SwipeArea はビューポート端から同じドロワーを開きます。" preview={<Stories.DrawerProviderHandlePreview />} sources={[{ label: 'React', language: 'tsx', code: Stories.drawerProviderHandleSourceJa }]} />

<ComponentExampleTabs id="drawer-virtual-keyboard" data-docs-example-item-count="1" title="仮想キーボード" description="VirtualKeyboardProvider は、モバイルの仮想キーボードによってビジュアルビューポートが変化しても、フォーカス中のフィールドを表示範囲内に保ちます。" preview={<Stories.DrawerVirtualKeyboardPreview />} sources={[{ label: 'React', language: 'tsx', code: Stories.drawerVirtualKeyboardSourceJa }]} />

## 構造

`Root` は state を所有し、`Trigger` は drawer を開きます。`Portal` は overlay を body または指定 container に mount します。`Backdrop`、`Viewport`、`Popup`、scroll 可能な `Content` が modal surface を構成します。`Title` と `Description` は dialog label を提供し、`Close` は閉じます。`SwipeArea` は edge opening を提供します。`Provider`、`Indent`、`IndentBackground` は nested page effect を調整します。`VirtualKeyboardProvider` は mobile keyboard viewport change を処理します。`createHandle` は typed payload を使う detached trigger を接続します。

## 動作

- controlled visibility には `open` と `onOpenChange`、uncontrolled visibility には `defaultOpen` を使います。
- controlled position には `snapPoint` と `onSnapPointChange`、uncontrolled position には `defaultSnapPoint` を使い、許可値を `snapPoints` に指定します。
- `swipeDirection` は edge と dismissal 方向を決めます。`snapToSequentialPoints` は velocity による point skipping を防ぎます。
- Escape、`Close`、backdrop press、non-modal focus-out、swipe が既定の dismissal です。outside pointer dismissal を防ぐには `disablePointerDismissal` を使います。
- `modal` の既定値は `true` です。outside interaction には `false`、scroll lock なしで focus のみを閉じ込めるには `trap-focus` を使います。
- `Popup` の `initialFocus` と `finalFocus` で focus 移動を指定します。既定では focus は drawer に入り trigger に戻ります。
- `actionsRef` は imperative `close` と `unmount` を提供します。通常の flow では declarative state を推奨します。
- `Portal` は client で mount され、閉じた SSR markup は hydration-safe です。`Viewport` は visual viewport と safe area 内に surface を保ちます。

## API

すべての compound part と prop/state type は `@tinyrack/ui/components/drawer` から export されます。CSS は `@tinyrack/ui/components/drawer.css` で opt-in し、`--tr-drawer-popup-max-width`、`--tr-drawer-popup-background`、`--tr-drawer-backdrop`、`--tr-drawer-control-background`、`--tr-drawer-motion-duration`、`--tr-drawer-motion-easing` で appearance と motion を override できます。
