---
title: "간격"
description: "관계에 따라 간격을 선택해요. 세부 정보는 가깝게, 컴포넌트는 여유 있게, 섹션은 작업 경계를 드러내요."
section: foundations
order: 3
---
import { TRCode } from '@tinyrack/ui/components/code';
import { TRTable } from '@tinyrack/ui/components/table';
import { tinyrackSpacing } from '@tinyrack/ui/core';

## 관계부터 선택

간격은 어떤 요소가 서로 연결되는지 설명해요. 숫자보다 관계를 먼저 선택하고 같은 단계의 요소에는 같은 간격을 반복하세요.

- **세부 간격 (`xs`–`sm`):** 아이콘과 레이블, 레이블과 값, 하나의 작은 그룹 안에 있는 줄
- **컴포넌트 간격 (`md`–`lg`):** 필드, 동작, 행, 경계가 있는 컴포넌트 내부 padding
- **섹션 간격 (`xl`–`2xl`):** 작업 그룹, 페이지 영역, 주제가 크게 바뀌는 경계

## 시각적 스케일

<div className="grid gap-3" role="list">{Object.entries(tinyrackSpacing).map(([name, value]) => (<div className="grid grid-cols-[3rem_minmax(0,1fr)_5rem] items-center gap-3" key={name} role="listitem"><TRCode>{name}</TRCode><span className="block h-3 bg-tinyrack-primary" style={{ width: value }} /><TRCode>{value}</TRCode></div>))}</div>

## 레이아웃 리듬

안쪽부터 바깥쪽으로 구성하세요. 레이블과 값은 `xs`로 묶고, 같은 단계의 행은 `sm`이나 `md`로 나누고, 컴포넌트 안쪽은 `lg`로 채우고, 다음 작업과는 `xl`이나 `2xl`로 분리해요. 모든 요소 쌍에 다른 간격을 만들지 마세요. 같은 관계는 같은 모양으로 보여야 해요.

<div className="grid gap-tinyrack-xl md:grid-cols-2"><section className="grid gap-tinyrack-md border border-tinyrack-border p-tinyrack-lg"><div className="grid gap-tinyrack-xs"><span className="text-tinyrack-xs text-tinyrack-text-muted">랙</span><strong>rack-a</strong></div><div className="grid gap-tinyrack-xs"><span className="text-tinyrack-xs text-tinyrack-text-muted">상태</span><strong>정상</strong></div></section><section className="grid content-start gap-tinyrack-sm"><h3 className="m-0">리듬</h3><p className="m-0 text-tinyrack-text-muted">세부 간격은 레이블과 값을 묶고, 컴포넌트 간격은 행을 나누며, 섹션 간격은 작업을 나눠요.</p></section></div>

## 구현과 참고

```css
.rack-card { display: grid; gap: var(--tinyrack-space-md); padding: var(--tinyrack-space-lg); }
```

<TRTable.Root containerProps={{ 'aria-label': '간격 토큰 참고', tabIndex: 0 }} data-foundation-reference="spacing"><thead><tr><th scope="col">토큰</th><th scope="col">값</th><th scope="col">일반 관계</th><th scope="col">Tailwind</th><th scope="col">TypeScript</th></tr></thead><tbody>{Object.entries(tinyrackSpacing).map(([token, value]) => (<tr key={token}><th scope="row"><TRCode>{token}</TRCode></th><td><TRCode>{value}</TRCode></td><td>{token === '3xs' || token === 'xs' || token === 'sm' ? '세부' : token === 'md' || token === 'lg' ? '컴포넌트' : '섹션'}</td><td><TRCode>{`gap-tinyrack-${token}`}</TRCode></td><td><TRCode>{`tinyrackSpacing['${token}']`}</TRCode></td></tr>))}</tbody></TRTable.Root>

[Tailwind 간격 매핑](/ko/foundations/tailwind/#spacing-controls)도 확인하세요.
