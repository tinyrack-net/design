---
title: "Link"
description: "native anchor 기능을 유지하면서 의미 있는 색상과 underline 동작을 제공하는 탐색입니다."
section: components
order: 14
---
import { ComponentPlayground } from '../../../playground/playground.js';
import { Link as RouterLink } from 'react-router';

import { TRLink } from '@tinyrack/ui/components/link';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/link.demo.js';

## 계약

| 축 | 값 | 기본값 |
| --- | --- | --- |
| `variant` | `default`, `muted`, `danger` | `default` |
| `underline` | `always`, `hover`, `none` | `hover` |
| `disabled` | pointer 및 keyboard 탐색 차단 | `false` |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/link.css';", "import { TRLink } from '@tinyrack/ui/components/link';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs id="link-basic" title="현재 목적지" description="일반적인 탐색에는 네이티브 링크를 사용하고 현재 목적지는 aria-current로 표시해요." preview={<TRLink data-docs-example-item="" aria-current="page" href="/racks">랙</TRLink>} sources={[{ label: 'React', language: 'tsx', code: Stories.linkBasicSource }]} />

## 예시

<ComponentExampleTabs id="link-router" title="클라이언트 라우터 조합" description="render로 라우터 링크를 조합하면 anchor를 중첩하지 않고 TRLink의 동작과 스타일을 적용할 수 있어요." preview={<TRLink data-docs-example-item="" render={<RouterLink to="/racks/new" />}>랙 만들기</TRLink>} sources={[{ label: 'React', language: 'tsx', code: Stories.linkRouterSource }]} />

<ComponentExampleTabs
  id="link-variant-underline-matrix"
  title="의미별 variant"
  description="일반 탐색에는 default, 보조 링크에는 muted, 파괴적인 결과로 이동할 때는 danger를 사용하세요."
  previewLayout="start"
  preview={(
    <div className="flex flex-wrap gap-4"><TRLink data-docs-example-item="" href="#" variant="default">기본</TRLink><TRLink data-docs-example-item="" href="#" variant="muted">보조</TRLink><TRLink data-docs-example-item="" href="#" variant="danger">위험</TRLink></div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: Stories.linkMatrixSource }]}
/>

<ComponentExampleTabs id="link-underlines" title="밑줄 동작" description="대부분은 기본 hover 밑줄을 사용하고, 탐색 가능성을 계속 보여야 할 때는 always를 사용하세요. 문맥만으로 링크가 분명할 때만 none을 사용하세요." preview={<div className="flex flex-wrap gap-4"><TRLink data-docs-example-item="" href="#" underline="hover">가리킬 때</TRLink><TRLink data-docs-example-item="" href="#" underline="always">항상</TRLink><TRLink data-docs-example-item="" href="#" underline="none">없음</TRLink></div>} sources={[{ label: 'React', language: 'tsx', code: Stories.linkUnderlinesSource }]} />

<ComponentExampleTabs
  id="link-destinations"
  title="내부·외부·다운로드 목적지"
  description="외부 link는 보이는 text로 새 browsing context를 알립니다. Download는 native anchor capability로 유지됩니다."
  preview={(
    <div className="flex flex-wrap gap-4">
      <TRLink data-docs-example-item="" href="#rack-inventory">랙 목록</TRLink>
      <TRLink data-docs-example-item="" href="https://tinyrack.net" rel="noreferrer" target="_blank" variant="default">Tinyrack 웹사이트(새 탭에서 열림)</TRLink>
      <TRLink data-docs-example-item="" disabled href="/billing">결제(비활성)</TRLink>
      <TRLink data-docs-example-item="" download="rack-inventory.csv" href="/rack-inventory.csv" underline="always">랙 목록 다운로드(CSV)</TRLink>
    </div>
  )}
  sources={[{ label: 'React', language: 'tsx', code: Stories.linkDestinationsSource }]}
/>

## API

탐색에는 `TRLink`, 명령에는 `TRButton`을 사용하세요. 활성 link는 Enter로 실행되며 Space는 native anchor처럼 scroll합니다. Disabled link는 `aria-disabled`를 노출하고 native 목적지와 tab stop을 제거하며 pointer, keyboard, programmatic activation을 막습니다. 외부 link가 새 browsing context를 열면 보이는 text로 알리고 `target="_blank"`와 적절한 `rel`을 함께 사용하세요.

| Prop | 타입 / 기본값 | 용도 |
| --- | --- | --- |
| `href` | native anchor `href` | 목적지를 지정하며 disabled일 때 생략됩니다. |
| `variant` | `default \| muted \| danger` / `default` | 의미 기반 text color를 선택합니다. |
| `underline` | `always \| hover \| none` / `hover` | underline 표시 방식을 선택합니다. |
| `disabled` | `boolean` / `false` | 탐색을 막고 순차 focus에서 제거합니다. |
| `render` | Base UI render element 또는 callback | nested anchor 없이 React Router 등의 link를 조합합니다. |
| native props | anchor attributes, events, `className`, `style`, `ref` | current state, 외부/download 속성, handler, style, React 19 ref를 전달합니다. |

### Style tokens

`--tr-link-color`, `--tr-link-font-weight`, `--tr-link-decoration-thickness`, `--tr-link-underline-offset`, `--tr-link-hover-opacity`, `--tr-link-disabled-opacity`, `--tr-link-radius`, `--tr-link-focus-color`, `--tr-link-focus-width`, `--tr-link-focus-offset`, `--tr-link-duration`으로 Tinyrack fallback을 유지하면서 component를 조정할 수 있습니다.
