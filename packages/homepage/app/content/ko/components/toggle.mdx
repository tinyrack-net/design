---
title: "Toggle"
description: "서식이나 뷰 옵션을 위한 2상태 button이에요. aria-pressed를 노출하며 form field가 아니고 자체 값을 제출하지 않아요."
section: components
order: 47
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { TRToggle } from '@tinyrack/ui/components/toggle';
import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/toggle.demo.js';

## 계약

| 축 | 값 | 기본값 |
| --- | --- | --- |
| state | controlled `pressed` or uncontrolled `defaultPressed` | off |
| availability | enabled or `disabled` | enabled |
| label | visible text | required |

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', imports: ["import '@tinyrack/ui/components/toggle.css';", "import { TRToggle } from '@tinyrack/ui/components/toggle';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="toggle-basic"
  title="제어형 서식 toggle"
  preview={<div data-docs-example-item-count="1"><Stories.ToggleInteractiveExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { useState } from 'react';
import { TRToggle } from '@tinyrack/ui/components/toggle';

export function ToggleExample() {
  const [pressed, setPressed] = useState(false);

  return (
    <div className="grid justify-items-start gap-3">
      <TRToggle onPressedChange={setPressed} pressed={pressed}>
        Bold
      </TRToggle>
      <output aria-live="polite" className="text-tinyrack-sm text-tinyrack-text-muted">
        Bold: {pressed ? 'on' : 'off'}
      </output>
    </div>
  );
}` }]}
/>

## 예시

<ComponentExampleTabs
  id="toggle-states"
  title="활성 및 비활성 상태"
  preview={<div data-docs-example-item-count="4"><Stories.ToggleStateComparison /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRToggle } from '@tinyrack/ui/components/toggle';

export function ToggleStates() {
  return (
    <div className="grid gap-4 sm:grid-cols-2">
      <div className="grid gap-2">
        <strong>Enabled · Off</strong>
        <TRToggle>Bold</TRToggle>
      </div>
      <div className="grid gap-2">
        <strong>Enabled · On</strong>
        <TRToggle defaultPressed>Italic</TRToggle>
      </div>
      <div className="grid gap-2">
        <strong>Disabled · Off</strong>
        <TRToggle disabled>Underline</TRToggle>
      </div>
      <div className="grid gap-2">
        <strong>Disabled · On</strong>
        <TRToggle defaultPressed disabled>
          Strikethrough
        </TRToggle>
      </div>
    </div>
  );
}` }]}
/>

## API

| Prop | 타입 | 기본값 | 용도 |
| --- | --- | --- | --- |
| `pressed` | `boolean` | — | pressed 상태를 제어해요. |
| `defaultPressed` | `boolean` | `false` | 비제어 상태의 초깃값을 설정해요. |
| `onPressedChange` | `(pressed, details) => void` | — | 요청된 상태 변경을 알려요. `details.cancel()`로 변경을 취소할 수 있어요. |
| `disabled` | `boolean` | `false` | 상호 작용과 순차 focus에서 button을 제외해요. |
| `value` | `string` | 자동 생성 | `TRToggleGroup` 안의 item을 식별해요. group에서는 안정적인 값을 제공하세요. |
| `className` | `string \| (state) => string` | — | consumer class를 추가하며 `TRToggleState`를 사용할 수 있어요. |
| `render` | `ReactElement \| (props, state) => ReactElement` | `<button>` | 병합된 동작을 유지하면서 렌더링 요소를 바꿔요. |

`TRToggleProps`와 `TRToggleState`는 `@tinyrack/ui/components/toggle`에서 export돼요. TRToggle은 button ref, style, native attribute와 event handler도 보존해요. native Space/Enter activation, focus, `disabled` 의미는 button 동작을 따르며 기본 button type은 둘러싼 form을 제출하지 않아요. 모든 toggle에 accessible name을 제공하세요. polymorphic `render`는 button keyboard 의미를 보존해야 해요. form 제출이나 required 검증에 값이 참여해야 하면 TRCheckbox 또는 TRSwitch를 사용하세요.
