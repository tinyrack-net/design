---
title: "Fieldset"
description: "관련 컨트롤을 접근 가능한 네이티브 legend 하나로 묶어요."
section: components
group: forms-inputs
---
import { ComponentPlayground } from '../../../playground/playground.js';

import { ComponentExampleTabs } from '../../../documentation/shared/component-example-tabs.js';
import { ComponentInstall } from '../../../documentation/shared/component-install.js';
import * as Stories from '../../../documentation/components/fieldset.demo.js';

## 핵심 속성

`Root`는 네이티브 `<fieldset>`을 렌더링해요. `Legend`는 기본적으로 네이티브 `<legend>`를 렌더링해 그룹에 접근 가능한 이름을 붙여요. 하위 컨트롤에는 저마다 고유한 표시 레이블이 필요해요. 컨트롤과 `TRField.Label`을 `TRField.Item`에 함께 두면 아이템이 그 안의 컨트롤에 레이블을 연결하니 `id`나 `htmlFor`가 필요 없어요. `TRField.Item`에는 `TRField.Root` 조상이 반드시 필요해요. 아이템을 하나의 `TRField.Root`로 묶어 `Root` 안에 두고, 필드셋의 간격을 유지하려면 그 루트에 `gap`을 지정하세요. `Root`에 `disabled`를 주면 네이티브 폼 컨트롤 비활성화가 하위로 전파돼요.

## 설치

<ComponentInstall surfaces={[{ label: 'React', install: 'pnpm add @tinyrack/ui', styleImports: ["@import '@tinyrack/ui/components/fieldset.css';"],
      codeImports: ["import { TRFieldset } from '@tinyrack/ui/components/fieldset';"] }]} />

## 플레이그라운드

<ComponentPlayground definition={Stories.playground} />

## 사용법

<ComponentExampleTabs
  id="fieldset-basic"
  title="알림 채널"
  preview={<Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts legend="알림" disabled={false} />}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function NotificationChannels() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>알림</TRFieldset.Legend>
      <TRField.Root className="gap-3">
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            이메일 알림을 받아요
          </TRField.Label>
        </TRField.Item>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            인시던트 요약을 받아요
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
    </TRFieldset.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

## 예시

<ComponentExampleTabs
  id="fieldset-states"
  title="활성 및 비활성 그룹"
  preview={<div className="grid gap-5 sm:grid-cols-2"><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled={false} legend="직접 변경하는 설정" /><Stories.FieldsetPreview data-docs-example-item="" defaultEmailAlerts disabled legend="관리되는 설정" /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

function NotificationSettings({
  disabled = false,
  legend,
}: {
  disabled?: boolean;
  legend: string;
}) {
  return (
    <TRFieldset.Root disabled={disabled}>
      <TRFieldset.Legend>{legend}</TRFieldset.Legend>
      <TRField.Root className="gap-3">
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            이메일 알림을 받아요
          </TRField.Label>
        </TRField.Item>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            인시던트 요약을 받아요
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
    </TRFieldset.Root>
  );
}

export function FieldsetStates() {
  return (
    <div className="grid gap-5 sm:grid-cols-2">
      <NotificationSettings legend="직접 변경하는 설정" />
      <NotificationSettings disabled legend="관리되는 설정" />
    </div>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

<ComponentExampleTabs
  id="fieldset-composition"
  title="중첩 그룹"
  preview={<div data-docs-example-item=""><Stories.FieldsetCompositionExample /></div>}
  sources={[{ label: 'React', language: 'tsx', code: String.raw`import { TRCheckbox } from '@tinyrack/ui/components/checkbox';
import { TRField } from '@tinyrack/ui/components/field';
import { TRFieldset } from '@tinyrack/ui/components/fieldset';

export function IncidentNotifications() {
  return (
    <TRFieldset.Root>
      <TRFieldset.Legend>인시던트 알림</TRFieldset.Legend>
      <TRField.Root>
        <TRField.Item className="items-center">
          <TRCheckbox.Root defaultChecked>
            <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
          </TRCheckbox.Root>
          <TRField.Label className="normal-case tracking-normal font-normal">
            인시던트 알림을 사용해요
          </TRField.Label>
        </TRField.Item>
      </TRField.Root>
      <TRFieldset.Root>
        <TRFieldset.Legend>전송 채널</TRFieldset.Legend>
        <TRField.Root className="gap-3">
          <TRField.Item className="items-center">
            <TRCheckbox.Root defaultChecked>
              <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
            </TRCheckbox.Root>
            <TRField.Label className="normal-case tracking-normal font-normal">
              이메일로 받아요
            </TRField.Label>
          </TRField.Item>
          <TRField.Item className="items-center">
            <TRCheckbox.Root>
              <TRCheckbox.Indicator aria-hidden="true">✓</TRCheckbox.Indicator>
            </TRCheckbox.Root>
            <TRField.Label className="normal-case tracking-normal font-normal">
              문자 메시지로 받아요
            </TRField.Label>
          </TRField.Item>
        </TRField.Root>
      </TRFieldset.Root>
    </TRFieldset.Root>
  );
}` ,
      styles: { code: "@import '@tinyrack/ui/core.css';\n@import '@tinyrack/ui/components/checkbox.css';\n@import '@tinyrack/ui/components/field.css';\n@import '@tinyrack/ui/components/fieldset.css';" },
    },
  ]}
/>

## API

`TRFieldsetRoot`와 `TRFieldsetLegend`는 개별적으로 내보내며, `TRFieldset.Root`와 `TRFieldset.Legend`로도 사용할 수 있어요.

| 파트 | 설명 |
| --- | --- |
| `Root` | 네이티브 fieldset 속성, `disabled`, `render`, `className`, `style`, 이벤트, ref |
| `Legend` | 기본적으로 네이티브 legend를 렌더링하며 `render`, `className`, `style`, 이벤트, ref 지원 |

네이티브 `<legend>` 배치가 적합하지 않을 때만 `render`를 사용하세요. 사용자 지정 legend도 Base UI가 `aria-labelledby`로 fieldset과 연결해요.
